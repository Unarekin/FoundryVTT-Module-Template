"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var import_path = __toESM(require("path"));
var fse = __toESM(require("fs-extra"));
var import_glob = require("glob");
const NAME = "json-merge";
const getContentChunks = (entryPoints) => Promise.all(
  entryPoints.filter((item) => item != null).map(
    (item, index) => typeof item === "string" ? (0, import_glob.glob)(item).then(
      (filenames) => Promise.all(
        filenames.sort().map((filename) => fse.readJSON(filename))
      )
    ) : Promise.resolve(
      index === 0 ? JSON.parse(JSON.stringify(item)) : item
    )
  )
).then((items) => items.flat().filter((item) => item != null));
const defaultMerge = (items) => (
  // eslint-disable-next-line @typescript-eslint/ban-ts-comment
  // @ts-ignore
  Object.assign(...items)
);
module.exports = ({
  entryPoints,
  outfile,
  merge = defaultMerge
} = {}) => ({
  name: NAME,
  async setup(build) {
    if (!outfile) {
      throw new Error(`[${NAME}] outfile option is missing`);
    }
    if (!Array.isArray(entryPoints)) {
      throw new Error(`[${NAME}] entryPoints option is not an array`);
    }
    if (!entryPoints.length)
      return;
    const destination = import_path.default.join(
      build.initialOptions.outdir || import_path.default.basename(build.initialOptions.outfile || ""),
      outfile
    );
    build.onStart(async () => {
      const chunks = await getContentChunks(entryPoints);
      const merged = chunks.length ? merge(chunks) : void 0;
      if (merged != null) {
        await fse.mkdirp(import_path.default.dirname(destination));
        await fse.writeJSON(destination, merged, { spaces: 2 });
      }
    });
  }
});
//# sourceMappingURL=index.js.map
